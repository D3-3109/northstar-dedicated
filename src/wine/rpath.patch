diff --git a/configure b/configure
--- a/configure
+++ b/configure
@@ -13122,6 +13122,8 @@ printf '%s\n' "$ac_cv_cflags__Wl___export_dynamic" >&6; }
 if test "x$ac_cv_cflags__Wl___export_dynamic" = xyes
 then :
   WINELOADER_LDFLAGS="-Wl,--export-dynamic"
 fi
+    # Keep Unix Wine libraries able to locate their siblings after installation.
+    UNIXLDFLAGS="$UNIXLDFLAGS '-Wl,-rpath,\$\$ORIGIN'"
     WINEPRELOADER_LDFLAGS="-nostartfiles -nodefaultlibs"
 
diff --git a/configure.ac b/configure.ac
--- a/configure.ac
+++ b/configure.ac
@@ -1094,5 +1094,7 @@ case $host_os in
     test $ac_cv_wl_z_defs != yes || AS_VAR_APPEND([UNIXLDFLAGS],[" -Wl,-z,defs"])
 
     WINE_TRY_CFLAGS([-Wl,--export-dynamic],[WINELOADER_LDFLAGS="-Wl,--export-dynamic"])
+    dnl Keep Unix Wine libraries able to locate their siblings after installation.
+    UNIXLDFLAGS="$UNIXLDFLAGS '-Wl,-rpath,\$\$ORIGIN'"
     WINEPRELOADER_LDFLAGS="-nostartfiles -nodefaultlibs"
 
